import 'package:flutter/material.dart';

class SavedMoviesList extends StatelessWidget {
  const SavedMoviesList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Saved movies list"),
    );
  }
}
